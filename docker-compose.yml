version: '3.8'
services:
  app:
    build: .
    container_name: dex-amm-evaluation
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=test
    command: tail -f /dev/null